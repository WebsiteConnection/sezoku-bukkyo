---
interface Props {
  author: string;
  socialLinks: { text: string; href: string }[];
}

const { author, socialLinks } = Astro.props;
---

<div class="flex flex-col gap-2">
  <p class="text-sm text-neutral-400">
    &copy; {new Date().getFullYear()} {author}. All rights reserved.
  </p>
  <a 
    href="mailto:your-email@example.com" 
    class="text-xs text-neutral-500 hover:text-primary transition-colors w-fit"
  >
    secular-lotus@gmail.com
  </a>
</div>

    <div class="flex gap-4">
      {
        socialLinks.map((link) => (
          <a
            href={link.href}
            class="text-sm text-neutral-400 transition-colors hover:text-white"
            target="_blank"
            rel="noopener noreferrer"
          >
            {link.text}
          </a>
        ))
      }
    </div>
  </div>
</footer>
